function onCreate()
	--Background stuff
	makeLuaSprite('LeoBG2','Stage 2',0, 0);
	addLuaSprite('LeoBG2',false);
	scaleObject('LeoBG2', 0.8, 0.8);
	setLuaSpriteScrollFactor('LeoBG2', 1, 1);
	close(true);
end

function onUpdate(elapsed)

end

function onBeatHit( ... )--for every beat
	-- body
end

function onStepHit( ... )-- for every step
	-- body
end

function onUpdate( ... )
    -- body
end




function onBeatHit( ... )--for every beat
	-- body
end

function onStepHit( ... )-- for every step
	-- body
end

function onUpdate( ... )
    -- body
end
