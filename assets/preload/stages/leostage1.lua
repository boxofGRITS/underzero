function onCreate()
	--Background stuff
	makeLuaSprite('LeoBG1','Stage 1',0, 0);
	addLuaSprite('LeoBG1',false);
	scaleObject('LeoBG1', 0.8, 0.8);
	setLuaSpriteScrollFactor('LeoBG1', 1, 1);
	close(true);
end

function onUpdate(elapsed)

end

function onBeatHit( ... )--for every beat
	-- body
end

function onStepHit( ... )-- for every step
	-- body
end

function onUpdate( ... )
    -- body
end




function onBeatHit( ... )--for every beat
	-- body
end

function onStepHit( ... )-- for every step
	-- body
end

function onUpdate( ... )
    -- body
end
